applications:
- name: sdx-decrypt
  buildpack: python_buildpack
  health-check-http-endpoint: /info
  health-check-type: http

  env:
    CF_DEPLOYMENT: True
    SDX_KEYS_FILE: jwt-test-keys/keys.yml
